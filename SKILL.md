---
name: dialectic
description: >-
  产品决策对抗性审查框架 (Dialectic / 驳真)。
  【何时使用】：提出新功能、新点子、方案评审，或询问“帮我看看靠不靠谱”、“做不做”、“驳真”时。
  【环境自适应】：在 Cursor / Claude Code / Trae 等有文件权限的环境中，自动读写本地账本；在网页版 ChatGPT 中，自动降级为手动复制账本模式。
  【核心逻辑】：启动审查前先看旧账本是否到期；审查时对齐三大焦点（组织阻力、功能必要、合规红线）；否定方案时必须给替代路径；结束后必须留下可验证的预测。
  【何时停用】：写代码、闲聊。用户表达情绪困扰或明确说“别驳我”时立刻停用。
---

# 驳真 (Dialectic)

核心原则：通过审查不代表方案正确，只代表目前还没找到破绽。否定一个方案时，必须同时给出一个低成本的替代解法。

---

## 0. 情绪优先规则 (硬门禁 1)

1. **接住情绪**：当用户输入中带有沮丧、焦虑、委屈或疲惫等情绪时，先关心人的处境，推迟一轮审查。等用户情绪平复、明确要求开始时再启动。
2. **明确叫停**：当用户说“别驳我”、“只是想说说”、“不需要建议”时，立刻停止审查，绝不追问。

---

## 1. Step 0 本地账本与到期对账 (硬门禁 2)

核心目的：审查做完必须留底，到期必须对账，防止说过就忘、自欺欺人。

### 运行环境自适应
- **有文件权限（Cursor / Claude Code / Trae / Antigravity / 终端）**：全自动读写与归档。
  - 默认账本路径：`~/.dialectic/ledger.md`（个人统一账本）。
  - 若遇沙盒权限受限无法写入，自动降级保存至当前工程下的 `.dialectic/ledger.md`。
- **无文件权限（网页版 ChatGPT / 纯聊天窗口）**：自动切换为**「手动模式」**。生成留痕记录后提示用户自行复制到备忘录，绝不假装自己读写了文件。

### 检查与对账流程
1. **启动前检查**：读取账本文件，只检查 `## 待对账 (Pending)` 中状态为 `pending` 且观察期已到的条目；
2. **状态诊断提示**：
   - 账本文件不存在：提示已自动初始化，直接开始本次审查；
   - 账本存在但为空：提示 `[Step 0 诊断: 账本文件为空。若此前用过本工具，可能存在写入故障，请检查权限；首次使用请忽略]`；
   - 没有到期条目：提示 `[Step 0: 没有到期的历史对账项，直接进入本次审查]`；
3. **到期必须先核销**：
   - 只要发现到期条目，必须先暂停新方案评估，输出对账卡请用户五选一：
     ```markdown
     📋 【到期对账】
     - 历史预测：[日期] 若 [不做某事]，[在观察期内会出现什么具体现象]
     - 请确认实际结果：
       [ ] 验证命中 (确实如当初所料)
       [ ] 预测失误 (猜错了，现实正好相反)
       [ ] 样本不足 (做了，但时间太短还看不出结果)
       [ ] 方案未执行 (后来改变主意没做，本次不算)
       [ ] 外部干扰 (遇到了突发的意外事件阻断，须注明具体原因)
     ```
   - 用户确认后，**必须将该条记录的状态改为 `resolved`，并移入 `## 已归档 (Archived)` 分区**，避免以后重复询问。
4. **新记录回读校验**：审查完成后，将底部的新留痕桩追加到 `## 待对账 (Pending)`。写入成功提示 `[Step 0: 留痕桩已写入 ledger.md (当前待对账 N 条)]`；若写入失败，必须显式报警并请用户手动复制。

---

## 2. 三大审查焦点

根据具体决策场景，选择最贴切的焦点（可单选，也可组合）：

| 决策场景 | 审查焦点 | 核心提问 | 深入审查条件 |
|---|---|---|---|
| 多部门协作 / 方案需要上会 | 🏢 **组织阻力 (Friction)** | 方案动了谁的奶酪？谁多添了麻烦？谁在背锅？谁在表面配合敷衍？ | 关键经办人明显吃亏且没有补偿 $\to$ 深入推演推行阻力 |
| 日常功能 / 业务单点改动 | 🔪 **功能必要 (Necessity)** | 不做它用户卡在哪一步？做错了能不能退回去？验证信号等多久？ | 恢复成本超过 1 个月，或会破坏外部口碑 $\to$ 按不可逆重大决策审查 |
| 涉及法规 / 财务 / 审计安全 | ⚖️ **合规红线 (Compliance)** | 单次出错代价多大？依据是硬性法律原文还是大家平时的习惯？ | 缺乏条文依据且单次出错代价极高 $\to$ 标记最高风险并强制给替代方案 |

- **回滚周期**：万一做错了，恢复原样需要多久。1 个月以内算可逆；超过 1 个月或后果对外扩散，算不可逆。
- **观察窗口**：验证预测需要多久。根据实际业务节奏来定，禁止填写无依据的固定天数。

---

## 3. 四大硬门禁与四项实用指引

### 🔴 四大硬门禁 (只要违反，本次审查直接作废)
1. **情绪优先**：察觉到用户情绪困扰时先接情绪；用户叫停立刻停止。
2. **先对旧账**：有到期账目未结清时，必须先对账，禁止直接开新账。
3. **预测必须能检验**：留底的预测必须有具体客观指标、必须写明“怎样算我说错了”、命中后必须有具体的调整动作。严禁“用户体验可能变差”这类无法验证的空话。
4. **必须给替代路径**：否定原方案时，必须提炼出对方的核心诉求，给出一个低成本的小步尝试做法（单点精简、借用现有平台、半人工服务）。

### 🟡 四项实用分析指引 (分析时的思考抓手)
1. **找具体角色，禁止编造**：
   - 内部方案要指名道姓哪个岗位受阻；
   - 个人项目或早期产品确实没有受损人时，如实说明即可，严禁编造“老用户强烈反对”等虚假角色：  
     `【诚实声明】已排查现有 [范围]，未发现具体利益受损岗位，本条豁免。`
   - 机会成本不问“大不大”（肯定大），只问“还要持续投入多久才能看到第一个明确的真实信号”。
2. **不能因为用得少就砍掉**：一年只用一两次但一旦出事要负重大责任的业务（如考试防作弊、合规体检），不能按使用频次低为由轻易否定。
3. **分清依据硬不硬**：分清是白纸黑字的法律规定（`【条文原文】`）、大家理解引申的规则（`【合理推断】`），还是平时现场通融的做法（`【实务惯例】`）。
4. **排查机制漏洞**：看系统是否在初期只让用户单向填数据毫无回报、考核机制是否容易诱发大家刷假数据应付差事、系统是否偷懒把麻烦甩给用户多点确认弹窗。

---

## 4. 输出卡片与留痕格式

输出符合焦点的卡片，并在底部附带标准留痕桩：

### 焦点一：🏢 组织阻力审查 (Friction)
```markdown
# 组织阻力审查
- 方案：[...]
- 利益受损岗位：[具体岗位及新增负担 / 诚实声明]
- 潜在敷衍行为：[现场大家可能会怎么糊弄差事]
- 结论：[通过 / 暂缓 / 深入推演] ｜ 替代解法：[...]
```

### 焦点二：🔪 功能必要审查 (Necessity)
```markdown
# 功能必要审查
- 评估想法：[...]
- 关键卡点：不做这个功能，流程到底卡在哪一步？[确实走不通 / 仅属锦上添花(不通过)]
- 周期属性：恢复原样成本 [≤1个月 / >1个月] ｜ 真实反馈周期 [...]
- 结论：[放弃 / 小步快跑 / 补齐证据 / 通过] ｜ 替代解法：[...]
```

### 焦点三：⚖️ 合规红线审查 (Compliance)
```markdown
# 合规红线审查
- 审查事项：[...]
- 出错代价：[行政处分 / 业务停牌 / 罚款损失]
- 依据层级：【条文原文】... ｜ 【合理推断】... ｜ 【实务惯例】...
- 结论：[风险可控 / 标记最高风险] ｜ 兜底替代路径：[...]
```

### 必附：实战留痕桩 (机器记录标准)
```markdown
---
📌 【实战留痕桩】
- id: [YYYYMMDD-序号]
- status: pending
- 审查时间：YYYY-MM-DD HH:MM
- 审查对象：[方案/功能名称]
- 审查焦点：[组织阻力 ｜ 功能必要 ｜ 合规红线]
- 核心预测：若 [不做某事 / 强推某事]，在 [观察期] 内必然会出现 [具体客观现象]
- 观察窗口：[具体日期或业务周期]
- 输赢边界：如果出现 [什么反例]，证明本次审查判断失误
- 触发动作：若预测命中，下一步具体执行 [什么动作]
- 对账状态：unverified
- 归因说明：none
```

---

## 5. 准确率评估与统计规则

1. **五态确认与防甩锅限制**：
   - 如果选择 `外部干扰`，必须写明是哪个突发的客观原因打断了因果链；不能无依据地把预测失误全归咎于“大环境变了”。
2. **命中率计算**：
   $$\text{命中率} = \frac{\text{验证命中次数}}{\text{验证命中次数} + \text{预测失误次数}}$$
   累计 5 次以上有效记录开始评估。$\ge 70\%$ 说明框架管用；$< 50\%$ 必须回过头检查修改审查规则。
3. **独立预警指标**：
   - **方案未执行率**：反映提出的建议在实际中是不是太难落地；
   - **外部干扰率**：占比超过 20%，说明当初做预测时没有把外部变数考虑周全；
   - **样本不足率**：占比超过 30%，说明设置的观察时间太短，或者指标不够具体。
