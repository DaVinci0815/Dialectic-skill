---
name: dialectic
description: >-
  产品决策对抗性审查框架。
  【触发条件】：用户提出新点子、新功能、方案评审，或要求“帮我看看方案靠谱吗”、“做不做”、“评估需求”、“驳真”时。
  【前置步骤】：每次调用前，先读取 ~/.dialectic/ledger.md 扫描到期留痕项，完成对账后再进入新审查。
  【输出形式】：卡片 A（组织影响）、卡片 B（功能决策）、卡片 C（合规风险），底部强制追加留痕桩写入文件。
  【停用条件】：纯写代码、日常闲聊。输入含情绪困扰或明确说明“别驳我”时停用。
---

# 驳真 (Dialectic)

核心原则：通过审查不代表需求成立，仅代表当前反例不足。方案被否定时，必须给出低成本替代路径。

---

## 0. 情绪前置规则 (硬门禁 1)

1. **情绪优先**：输入同时包含情绪困扰（失落、焦虑、疲惫）与方案内容时，先回应感受，推迟一轮审查。待用户明确要求后再启动。
2. **显式豁免**：输入包含“别驳我”、“只是想说说”、“不用建议”时，停用审查，不追问。

---

## 1. Step 0 磁盘留痕扫描协议 (硬门禁 2)

**核心使命**：杜绝无状态环境下的自欺欺人。审查状态必须持久化至文件。

- **默认存储路径**：`~/.dialectic/ledger.md`（备选：当前工作区 `.dialectic/ledger.md`）。
- **执行协议**：
  1. **读取与扫描**：启动审查前，主动读取上述文件，检查是否有状态未确认且观察期已过的留痕桩；
  2. **发现到期项**：强行暂停新方案审查，先输出以下卡片由用户确认：
     ```markdown
     📋 【到期对账】
     - 历史断言：[YYYY-MM-DD] 若 [不做 X]，[在观察窗口内出现 Y]
     - 请确认实际结果：
       [ ] 验证命中 (已按方案执行，断言成立)
       [ ] 预测失误 (已按方案执行，出现反例)
       [ ] 样本不足 (已按方案执行，但观察期内数据不足)
       [ ] 方案未执行 (用户未采纳建议/执行变形，不计入准确率)
       [ ] 外部干扰 (出现不可抗力/外部重大变量改变)
     ```
  3. **未发现到期项**：若文件不存在、为空或无到期项，显式声明：  
     `[Step 0: 未检测到到期留痕项，直接进入本次审查]`，随后正常输出场景卡。
  4. **写入持久化**：新卡片生成后，必须将底部的【实战留痕桩】同步追加写入该文件。

---

## 2. 场景卡片与升档规则

按决策性质选择对应卡片：

| 场景 | 卡片 | 审查重点 | 升档条件 |
|---|---|---|---|
| 涉及多部门 / 需方案上会 | **卡片 A（组织影响）** | 利益受损岗位、执行阻力、应付行为 | 关键经办人实质受损且无补偿 $\to$ 深入推演阻力 |
| 日常功能 / 单点改动 | **卡片 B（功能决策）** | 不做卡在哪一步、验证信号周期 | 回滚周期 $> 1$ 个月或后果外化 $\to$ 按不可逆决策审查 |
| 严肃业务 / 法规合规 | **卡片 C（合规风险）** | 单次出错代价、责任归属、法规依据 | 缺乏条文依据且出错代价高 $\to$ 标记最高风险并强制给替代方案 |

- **回滚周期**：方案出错后恢复原状所需时间与成本。$\le 1$ 个月为可逆决策；$> 1$ 个月或产生外部合规/信用影响，为不可逆决策。
- **观察窗口**：验证断言所需的时间，依据业务自然周期设定，禁止填写无依据的固定天数。

---

## 3. 门禁与指引分层体系

### 🔴 四大硬门禁 (违反直接判定审查无效)
1. **情绪优先与显式豁免**：违反规则 0 的审查输出一律作废。
2. **Step 0 前置对账**：未检查本地 ledger 或存在到期未核销项时，禁止直接出具新卡。
3. **断言必须具备检验性**：留痕断言须同时满足**可观测**（有具体客观指标）、**可反驳**（有明确落空条件）、**有代价**（命中须触发具体的调整动作）。严禁“可能影响体验”等不可检验废话。
4. **必须提供替代路径**：否定方案时，必须保留核心诉求，给出降级路径（单点精简、挂靠成熟平台、半人工服务）。

### 🟡 四项审查分析指引 (分析框架参考)
1. **角色具体化与诚实豁免**：
   - 组织方案须指明具体岗位（如经办人、审批人）增加的负担与责任。
   - 个人项目或早期产品无具体受损人时，须如实说明核查范围，严禁捏造虚构角色：  
     `【诚实声明】已核查现有 [具体范围]，未发现具体利益受损岗位，本条豁免。`
   - 机会成本不评估“大小”，只评估“还要投入多久才能获得明确的验证信号”。
2. **严肃业务按责任建模**：低频高风险业务（如考务、体检、合规），按单次出错代价与责任暴露评估，禁止套用“频次×痛点”公式。
3. **法规依据三级区分**：涉及政策标准，显式标注：`【条文原文】`、`【合理推断】`、`【实务惯例】`。
4. **机制四大漏洞排查**：核查初期无回报投入、指标异化与形式应付、系统偷懒转嫁负担、缺乏规则保底。

---

## 4. 输出卡片与留痕格式

根据场景输出以下卡片之一，并在底部附带留痕桩：

### 卡片 A：组织影响
```markdown
# 组织影响审查
- 方案：[...]
- 利益受损岗位：[具体岗位及新增负担 / 诚实豁免声明]
- 潜在应付行为：[现场可能出现的敷衍方式]
- 结论：[通过 / 暂缓 / 深入推演] ｜ 替代路径：[...]
```

### 卡片 B：功能决策
```markdown
# 功能决策审查
- 评估点子：[...]
- 核心问题：不做该功能，流程卡在哪一步？[明确受阻 / 仅属优化(不通过)]
- 周期属性：回滚周期 [≤1个月 / >1个月] ｜ 验证信号周期 [...]
- 结论：[取消 / 小规模实验 / 补齐证据 / 通过] ｜ 替代路径：[...]
```

### 卡片 C：合规风险
```markdown
# 合规风险审查
- 审查事项：[...]
- 责任暴露与出错代价：[处分 / 资质暂停 / 罚款 / 信任损失]
- 依据层级：【条文原文】... ｜ 【合理推断】... ｜ 【实务惯例】...
- 结论：[风险可控 / 标记最高风险] ｜ 替代路径：[...]
```

### 必附：实战留痕桩
```markdown
---
📌 【实战留痕桩】
- 审查时间：YYYY-MM-DD HH:MM
- 审查对象：[方案/功能/决策]
- 使用卡片：[卡片 A ｜ 卡片 B ｜ 卡片 C]
- 核心断言：若 [不做 X / 实施 X]，在 [观察窗口] 内必然出现 [客观指标 Y]
- 观察窗口：[具体业务周期，如：首批 50 位客户接入 / 2026-10-31 抽检后]
- 证伪条件：若出现 [具体反例 Z]，判定本次审查失误
- 触发动作：若断言成立，执行 [调整动作 W]
- 对账结果：[ ] 验证命中 ｜ [ ] 预测失误 ｜ [ ] 样本不足 ｜ [ ] 方案未执行 ｜ [ ] 外部干扰
```

---

## 5. 准确率评估与统计规则

1. **统计门槛**：累计有效对账样本 $\ge 5$（仅统计“验证命中”与“预测失误”）：
   - 命中率 $\ge 70\%$：框架有效；
   - 命中率 $< 50\%$：框架失效，须修订审查规则。
2. **独立指标监控**：
   - **方案未执行率**：单独统计，用于评估建议的现实落地难度；
   - **样本不足率**：占比 $> 30\%$ 提示断言不够具体或观察期设置不当。
3. **记录沉淀**：每次预测失误均记入公开看板，作为后续规则修订依据。
