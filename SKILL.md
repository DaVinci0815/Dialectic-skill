---
name: dialectic
description: >-
  产品决策对抗性审查框架。
  【触发条件】：用户提出新点子、新功能、方案评审，或要求“帮我看看方案靠谱吗”、“做不做”、“评估需求”、“驳真”时。
  【载体自适应】：有文件环境（Cursor/Claude Code/Trae/Antigravity/终端）优先读写当前工作区 .dialectic/ledger.md（沙盒友好），兜底使用全局 ~/.dialectic/ledger.md；无文件环境（ChatGPT Web）自动降级为手动台账模式。
  【前置步骤】：执行 Step 0 扫描到期留痕项（完成对账归档）并反哺检索历史归档踩坑记录，再进入新审查。
  【输出形式】：自解释三大审查维度（组织影响、功能决策、合规风险），底部追加留痕桩并回读确认。
  【停用条件】：纯写代码、日常闲聊。输入含情绪困扰或明确说明“别驳我”时停用。
---

# 驳真 (Dialectic)

核心原则：通过审查不代表需求成立，仅代表当前反例不足。方案被否定时，必须给出低成本替代路径。

---

## 0. 情绪前置规则 (硬门禁 1)

1. **情绪优先**：输入同时包含情绪困扰（失落、焦虑、疲惫）与方案内容时，先回应感受，推迟一轮审查。待用户明确要求后再启动。
2. **显式豁免**：输入包含“别驳我”、“只是想说说”、“不用建议”时，停用审查，不追问。

---

## 1. Step 0 留痕对账与载体降级协议 (硬门禁 2)

**核心使命**：杜绝无状态环境下的自欺欺人与死循环对账。

### 载体环境自适应
- **文件载体 (Cursor / Claude Code / Trae / Antigravity / 命令行)**：完整执行文件级自动读写与归档。
- **无文件载体 (ChatGPT Custom Instructions / 纯 Web 聊天)**：自动降级为**「手动台账模式」**。模型输出留痕桩后提示用户手动复制存档；会话初提示用户可粘贴未核销记录。

### 磁盘存储规范 (文件载体)
- **文件路径机制**：
  1. **工作区优先 (推荐)**：优先读取/创建当前项目工作区根目录下的 `.dialectic/ledger.md`（项目级独立生命周期、随代码版本管理、对容器/沙盒环境 100% 免疫权限问题）。
  2. **全局兜底**：若非工程目录，回退至全局 `~/.dialectic/ledger.md`；若遭遇沙盒权限受限（EACCES / Operation not permitted），自动优雅降级回当前工作区 `.dialectic/ledger.md`。
- **文件分区结构**：必须包含 `## 待对账 (Pending)` 与 `## 已归档 (Archived)` 两个分区。

### 读协议与诊断区分
1. **启动前检查 (两项扫描)**：
   - **扫描 A (到期对账)**：扫描 `## 待对账 (Pending)` 分区中 `status: pending` 且观察期已到的条目；
   - **扫描 B (历史教训反哺 / Memory Callback)**：扫描 `## 已归档 (Archived)` 分区，检索是否曾有与当前审查方案相关的历史踩坑/验证条目。若存在，在审查前输出 `💡 【历史踩坑唤醒】: 历史记录显示曾有类似决策导致 [历史结果]，请前置纳入本次推演`，防止在同一个坑摔倒两次。
2. **状态诊断与降级声明**：
   - 文件不存在：提示已自动初始化，直接进入本次审查；
   - 文件存在但为空：显式告警 `[Step 0 诊断: ledger 文件为空。若此前曾使用本框架，可能存在写入失败或权限异常，请检查；若首次使用请忽略]`；
   - 无到期项：声明 `[Step 0: 未检测到到期留痕项，直接进入本次审查]`；
3. **到期阻断与核销回写**：
   - 发现到期项时，强制暂停新方案审查，输出对账卡由用户五态确认：
     ```markdown
     📋 【到期对账】
     - 历史断言：[YYYY-MM-DD] 若 [不做 X]，[在观察窗口内出现 Y]
     - 请确认实际结果：
       [ ] 验证命中 (已按方案执行，断言成立)
       [ ] 预测失误 (已按方案执行，出现反例)
       [ ] 样本不足 (已按方案执行，但观察期内数据不足)
       [ ] 方案未执行 (用户未采纳建议/执行变形，不计入准确率)
       [ ] 外部干扰 (出现不可抗力阻断因果，须填写具体阻断原因)
     ```
   - 用户确认后，**模型必须回写文件**：将该条 `status` 字段改为 `resolved`，记录对账结论并**移入 `## 已归档 (Archived)` 分区**，防止重复对账死循环。

### 写协议与回读确认
新审查维度报告输出后，必须将底部的【实战留痕桩】追加写入 ledger.md 的 `## 待对账 (Pending)` 分区，并进行校验：
- **写入成功**：回读确认后输出声明：`[Step 0: 留痕桩已写入 ledger.md (当前待对账 N 条)]`；
- **写入失败**：严禁静默吞掉，必须显式报错：`[写入告警: 留痕桩写入 ledger.md 失败，请手动将下方留痕桩追加至账本文件]`。

---

## 2. 审查维度与升档规则

根据决策性质对齐对应的审查维度（可单选或交叉多选）：

| 决策场景 | 审查维度 | 审查核心痛点 | 升档与深入条件 |
|---|---|---|---|
| 涉及多部门协作 / 需方案上会 | **组织阻力维度 (Org Friction)** | 利益受损岗位、执行阻力、应付行为 | 关键经办人实质受损且无补偿 $\to$ 深入推演推行阻力 |
| 日常功能 / 业务单点改动 | **功能必要性维度 (Feature Decision)** | 不做卡在哪一步、验证信号周期 | 回滚周期 $> 1$ 个月或后果外化 $\to$ 按不可逆决策审查 |
| 严肃业务 / 法规合规 / 敏感操作 | **合规红线维度 (Compliance Risk)** | 单次出错代价、责任归属、法规依据 | 缺乏条文依据且出错代价高 $\to$ 标记最高风险并强制给替代方案 |

- **回滚周期**：方案出错后恢复原状所需时间与成本。$\le 1$ 个月为可逆决策；$> 1$ 个月或产生外部合规/信用/不可逆影响，为不可逆决策。
- **观察窗口**：验证断言所需的时间，依据业务自然周期设定，禁止填写无依据的固定天数。

---

## 3. 门禁与指引分层体系

### 🔴 四大硬门禁 (违反直接判定审查无效)
1. **情绪优先与显式豁免**：违反规则 0 的审查输出一律作废。
2. **Step 0 前置对账与教训反哺**：未检查本地 ledger、存在到期未核销项或未检索历史归档教训时，禁止直接出具新报告。
3. **断言必须具备检验性**：留痕断言须同时满足**可观测**（有具体客观指标）、**可反驳**（有明确落空条件）、**有代价**（命中须触发具体的调整动作）。
4. **必须提供替代路径**：否定方案时，必须保留核心诉求，给出降级路径（单点精简、挂靠成熟平台、半人工服务）。

### 🟡 四项审查分析指引 (分析框架参考)
1. **角色具体化与诚实豁免**：
   - 组织方案须指名具体岗位（如经办人、审批人）增加的负担与责任。
   - 个人项目或早期产品无具体受损人时，须如实说明核查范围，严禁捏造虚构角色：  
     `【诚实声明】已核查现有 [具体范围]，未发现具体利益受损岗位，本条豁免。`
   - 机会成本不评估“大小”，只评估“还要投入多久才能获得明确的验证信号”。
2. **严肃业务按责任建模**：低频高风险业务（如考务、体检、合规），按单次出错代价与责任暴露评估，禁止套用“频次×痛点”公式。
3. **法规依据三级区分**：涉及政策标准，显式标注：`【条文原文】`、`【合理推断】`、`【实务惯例】`。
4. **机制四大漏洞排查**：核查初期无回报投入、指标异化与形式应付、系统偷懒转嫁负担、缺乏规则保底。

---

## 4. 输出维度报告与标准留痕格式

### 维度一：组织阻力审查 (Org Friction)
```markdown
# 组织阻力审查
- 方案：[...]
- 利益受损岗位：[具体岗位及新增负担 / 诚实豁免声明]
- 潜在应付行为：[现场可能出现的敷衍方式]
- 结论：[通过 / 暂缓 / 深入推演] ｜ 替代路径：[...]
```

### 维度二：功能必要性审查 (Feature Decision)
```markdown
# 功能必要性审查
- 评估点子：[...]
- 核心问题：不做该功能，流程卡在哪一步？[明确受阻 / 仅属优化(不通过)]
- 周期属性：回滚周期 [≤1个月 / >1个月] ｜ 验证信号周期 [...]
- 结论：[取消 / 小规模实验 / 补齐证据 / 通过] ｜ 替代路径：[...]
```

### 维度三：合规红线审查 (Compliance Risk)
```markdown
# 合规红线审查
- 审查事项：[...]
- 责任暴露与出错代价：[处分 / 资质暂停 / 罚款 / 信任损失]
- 依据层级：【条文原文】... ｜ 【合理推断】... ｜ 【实务惯例】...
- 结论：[风险可控 / 标记最高风险] ｜ 替代路径：[...]
```

### 必附：实战留痕桩标准格式 (机读规范)
```markdown
---
📌 【实战留痕桩】
- id: [YYYYMMDD-序号]
- status: pending
- 审查时间：YYYY-MM-DD HH:MM
- 审查对象：[方案/功能/决策]
- 审查维度：[组织阻力 ｜ 功能必要性 ｜ 合规红线]
- 核心断言：若 [不做 X / 实施 X]，在 [观察窗口] 内必然出现 [客观指标 Y]
- 观察窗口：[具体业务周期/日期]
- 证伪条件：若出现 [具体反例 Z]，判定本次审查失误
- 触发动作：若断言成立，执行 [调整动作 W]
- 对账结果：unverified
- 归因说明：none
```

---

## 5. 准确率评估与信誉防线

1. **五态对账与外部干扰举证**：
   - 选填 `外部干扰` 时，**必须强制补充因果阻断举证**（说明什么具体外生变量如何直接切断因果链），未举证者不得以此结案。
2. **准确率核心公式**：
   $$\text{命中率} = \frac{\text{验证命中}}{\text{验证命中} + \text{预测失误}}$$
   有效样本（命中+失误）$\ge 5$ 开始计算。$\ge 70\%$ 有效；$< 50\%$ 强制规则复盘。
3. **独立预警指标**：
   - **方案未执行率**：反映建议在现实中的可行性阻力；
   - **外部干扰率**：占比 $> 20\%$ 提示断言隔离外生变量能力不足，触发 Gate 3 修订；
   - **样本不足率**：占比 $> 30\%$ 提示断言不够具体或观察期过长。

